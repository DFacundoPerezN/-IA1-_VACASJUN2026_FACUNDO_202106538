services:

  mongodb:
    image: mongo:8
    container_name: smartbot-mongodb

    restart: always

    ports:
      - "27017:27017"

    environment:
      MONGO_INITDB_ROOT_USERNAME: faxx
      MONGO_INITDB_ROOT_PASSWORD: faxx90

    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: